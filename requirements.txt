# 装完先跑一次 preflight.py，它会报出本机实际状态并拦下跑不动的组合。
#
# 必需
pymupdf>=1.23.0
lxml>=4.9.0
python-docx>=1.1.0

# 切分图面板（panel_split.py）
pillow>=10.0.0
numpy>=1.24.0

# 能力门禁只检查以下两项，至少一项可用，否则 preflight.py exit 4：
#   1) pywin32 + Acrobat Pro（仅 Windows）
#   2) Skill 内 ocr/.venv 中的 RapidOCR 及 ocr/models 中的默认模型
# 本地 OCR 缺失时先征得用户同意，再运行 setup_ocr.py --install。
# 全局 RapidOCR 不参与自检放行；两条路径都不可用且未同意安装则停止。
# 不调用 Microsoft Word；DOCX 只是中间文件格式。
#
# Acrobat 直转 Word（pdf_to_docx.py，仅 Windows，走 Acrobat COM 接口）
# Acrobat Pro 全自动导出是默认第一步，装了它这条就该打开。
# pywin32>=306

# 扫描件 OCR + 面板标签校验。装 RapidOCR：https://github.com/RapidAI/RapidOCR
# 所有 OCR 只调用本 Skill 的 RapidOCR，模型使用 ONNX 格式的 PP-OCR 权重。
#
# 统一入口：python "<真实 Skill 目录>/setup_ocr.py" --install
# 安装器将 requirements-ocr.txt 装进 ocr/.venv，模型保存在 ocr/models。
# 在隔离环境中只安装 opencv-python，不碰全局 opencv-contrib-python。
# 实测版本 rapidocr 3.9.2 + onnxruntime 1.26.0（Python 3.13）。
# rapidocr>=3.9
