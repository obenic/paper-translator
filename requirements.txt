# 装完先跑一次 preflight.py，它会报出本机实际状态并拦下跑不动的组合。
#
# 必需
pymupdf>=1.23.0
lxml>=4.9.0
python-docx>=1.1.0

# 切分图面板（panel_split.py）
pillow>=10.0.0
numpy>=1.24.0

# 能力门禁只检查以下两项，至少一项可用，否则 preflight.py exit 4：
#   1) pywin32 + Acrobat Pro（仅 Windows）
#   2) RapidOCR 及其运行依赖
# 两者都没有时，必须先征得用户同意下载、安装 RapidOCR；未同意则停止。
# 不调用 Microsoft Word；DOCX 只是中间文件格式。
#
# Acrobat 直转 Word（pdf_to_docx.py，仅 Windows，走 Acrobat COM 接口）
# Acrobat Pro 全自动导出是默认第一步，装了它这条就该打开。
# pywin32>=306

# 扫描件 OCR + 面板标签校验。装 RapidOCR：https://github.com/RapidAI/RapidOCR
# 两个后端跑的是同一套 PP-OCR 权重
# （RapidOCR 是把百度的模型转成 ONNX，版权仍归百度），所以这不是精度取舍，
# 而是同一个模型的两种跑法。默认流程只调用 RapidOCR。
#
# 装它。约 40MB（onnxruntime 常常已被别的包带进来），
# 实测同权重快 7.9 倍、用它更轻的默认档快 18.7 倍。
# --no-deps 是必须的：它声明依赖 opencv_python，而多数环境装的是
# opencv-contrib-python，两者争同一个 cv2 目录。contrib 版供的 cv2 完全够用
# （实测换过去 6 页输出逐字一致）。
#   pip install --no-deps rapidocr
#   pip install onnxruntime shapely pyclipper omegaconf colorlog numpy pillow requests tqdm six PyYAML python-docx
# cv2 缺失时另装 opencv-python；已有 cv2 则不安装或替换 OpenCV。
# 实测版本 rapidocr 3.9.2 + onnxruntime 1.26.0（Python 3.13）。
# rapidocr>=3.9

# PaddleOCR：ocr_engine.py 仅保留显式兼容调用，不参与自检或默认流程。
# 历史基准中它慢一个数量级、大一个数量级（约 1GB）、
# 精度不占优，新装没有理由选它。
# 实测版本 paddleocr 3.7.0 + paddlepaddle 3.3.1 (Python 3.13)。
#   pip install paddlepaddle paddleocr
# ocr_engine.py 内部强制 enable_mkldnn=False，绕过部分 paddle 构建的 oneDNN 崩溃。
# paddlepaddle>=3.0
# paddleocr>=3.0
